%h3 Registration Information
- send("show_#{@registration.type.downcase}_fields").each do |attribute|
  = render :partial => partials_path(@registration, 'field'), :object => @registration, :locals => {:attribute => attribute}

%h3 Technical Information

- show_technical_fields.each do |attribute|
  = render :partial => partials_path(@registration, 'field'), :object => @registration, :locals => {:attribute => attribute}

= link_to 'Edit', edit_polymorphic_path(@registration)
\|
= link_to 'Back', polymorphic_path(@registration.class)
